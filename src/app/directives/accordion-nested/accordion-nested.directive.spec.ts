import { AccordionNestedDirective } from './accordion-nested.directive';

describe('AccordionNestedDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionNestedDirective();
    expect(directive).toBeTruthy();
  });
});
